volumes:
  - id: "scaleio_sds"
    label: "ScaleIO SDS Device"
    type: "partition"
    min_size: 0
    mount: "none"
volumes_roles_mapping:
  scaleio_sds:
    - {allocate_size: "all", id: "scaleio_device"}
    
